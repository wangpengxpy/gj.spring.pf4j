<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.github.wangpengxpy</groupId>
    <artifactId>gj-plugin-build-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <properties>
        <!-- Exclude groupIds already provided by the host application -->
        <excludeGroupIds>
            com.ganweicloud,
            com.google.protobuf,
            com.google.j2objc,
            com.alibaba,
            com.github.yulichang,
            com.github.virtuald,
            com.baomidou,
            com.fasterxml.jackson.core,
            com.fasterxml.jackson.datatype,
            com.fasterxml.jackson.module,
            com.github.zafarkhaja,
            com.zaxxer,
            org.apache.tomcat,
            org.apache.poi,
            org.apache.commons,
            org.apache.xmlbeans,
            org.ehcache,
            org.springframework,
            org.springframework.boot,
            org.springdoc,
            org.webjars,
            org.mybatis,
            org.projectlombok,
            org.modelmapper,
            org.pf4j,
            org.apache.logging.log4j,
            org.slf4j,
            org.yaml,
            io.grpc,
            io.micrometer,
            io.swagger.core.v3,
            jakarta.servlet,
            jakarta.annotation,
            mysql,
            javax.annotation,
            ch.qos.logback,
            commons-io,
            commons-codec,
            io.netty,
            com.corundumstudio.socketio,
            org.jetbrains,
            net.lingala.zip4j,
            com.github.jsqlparser,
            org.springdoc,
            org.jboss.logging,
            com.fasterxml,
            jakarta.xml.bind,
            jakarta.validation,
            com.sun.activation,
            org.hibernate.validator
        </excludeGroupIds>
        <!-- Exclude groupIds already provided by the host application-->
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Execution 0: Copy dependencies -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <!-- Exclude groupIds already provided by the host application -->
                            <excludeGroupIds>${excludeGroupIds}</excludeGroupIds>
                            <!-- Exclude provided/test scope dependencies -->
                            <includeScope>runtime</includeScope>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Execution 1: Rebuild plugin directory -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <!-- Execution 0: Clean plugin output directory -->
                    <execution>
                        <id>clean-plugin-output-dir</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${project.build.directory}/plugins/${project.artifactId}"/>
                            </target>
                        </configuration>
                    </execution>
                    <!-- Execution 1: Copy JAR -->
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      tofile="${project.build.directory}/plugins/${project.artifactId}/${project.artifactId}-${project.version}.jar"/>
                            </target>
                        </configuration>
                    </execution>
                    <!-- Execution 2: Create empty lib directory -->
                    <execution>
                        <id>ensure-lib-dir</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/lib"/>
                            </target>
                        </configuration>
                    </execution>
                    <!-- Execution 3: Copy private libs -->
                    <execution>
                        <id>copy-src-lib</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <echo message="Checking for private jars in src/lib directory..."/>
                                <!-- failonerror="false" ensures the build continues even if the directory does not exist -->
                                <copy todir="${project.build.directory}/lib" overwrite="true" failonerror="false"
                                      verbose="true">
                                    <fileset dir="${project.basedir}/src/lib" includes="**/*.jar"
                                             erroronmissingdir="false"/>
                                </copy>
                                <!-- Check if any files were copied -->
                                <resourcecount property="jars.copied.count">
                                    <fileset dir="${project.build.directory}/lib" includes="**/*.jar"/>
                                </resourcecount>
                                <condition property="jars.were.copied">
                                    <not>
                                        <equals arg1="${jars.copied.count}" arg2="0"/>
                                    </not>
                                </condition>
                                <condition property="final.message"
                                           value="Successfully copied ${jars.copied.count} private jars from src/lib"
                                           else="No private jars to copy">
                                    <isset property="jars.were.copied"/>
                                </condition>
                                <echo message="${final.message}"/>
                            </target>
                        </configuration>
                    </execution>
                    <!-- Execution 4: Copy lib directory -->
                    <execution>
                        <id>copy-dependency-lib</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- Copy dependencies resolved by maven-dependency-plugin -->
                                <copy todir="${project.build.directory}/plugins/${project.artifactId}/lib">
                                    <fileset dir="${project.build.directory}/lib" includes="**/*.jar"/>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                    <!-- Execution 5: Generate MANIFEST.MF (only JARs under lib/) -->
                    <execution>
                        <id>generate-manifest</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/generated-manifest"/>

                                <!-- Create fileset containing all jar files -->
                                <fileset id="all.lib.jars" dir="${project.build.directory}/lib" includes="*.jar"
                                         erroronmissingdir="false"/>

                                <!-- Generate classpath string, space-separated -->
                                <pathconvert property="classpath.value" refid="all.lib.jars" pathsep=" ">
                                    <map from="${project.build.directory}/lib/" to="lib/"/>
                                </pathconvert>

                                <!-- Use Ant manifest task for automatic long-line wrapping -->
                                <manifest file="${project.build.directory}/generated-manifest/MANIFEST.MF">
                                    <attribute name="Manifest-Version" value="1.0"/>
                                    <attribute name="Class-Path" value="${classpath.value}"/>
                                    <attribute name="Plugin-Id" value="${project.artifactId}"/>
                                    <attribute name="Plugin-Version" value="${project.version}"/>
                                </manifest>

                                <!-- Debug info -->
                                <echo message="Class-Path value: ${classpath.value}"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Execution 2: Copy resource files -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <!-- Execution 0: Copy .json files -->
                    <execution>
                        <id>copy-plugin-json</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/plugins/${project.artifactId}
                            </outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/resources</directory>
                                    <includes>
                                        <include>${project.artifactId}.json</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <!-- Execution 1: Copy Localization directory (if exists) -->
                    <execution>
                        <id>copy-localization-resources</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/plugins/${project.artifactId}
                            </outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/resources</directory>
                                    <includes>
                                        <include>Localization/**</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Execution 3: 构建MANIFEST.MF -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifestFile>${project.build.directory}/generated-manifest/MANIFEST.MF</manifestFile>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
        <resources>
            <!-- Regular resources: no filtering -->
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>plugin.properties</exclude>
                </excludes>
            </resource>
            <!-- Only plugin.properties with filtering -->
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>plugin.properties</include>
                </includes>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>